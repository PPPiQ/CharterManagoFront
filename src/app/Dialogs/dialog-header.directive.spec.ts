import { DialogHeaderDirective } from './dialog-header.directive';

describe('DialogHeaderDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new AppDialogHeaderDirective();
  //   expect(directive).toBeTruthy();
  // });
});
