import { DialogFooterDirective } from './dialog-footer.directive';

describe('DialogFooterDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new AppDialogFooterDirective();
  //   expect(directive).toBeTruthy();
  // });
});
